{
  "name": "pmg-driver-app",
  "private": true,
  "version": "69.0.0",
  "scripts": {
    "test:unit": "for f in tests/test_*.js; do node \"$f\" || exit 1; done",
    "test:browser": "playwright test tests/driver_app_browser_audit.spec.js tests/plant_alert_acknowledgement.spec.js --reporter=line",
    "test": "npm run test:unit && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "wrangler": "4.103.0"
  }
}
